spring.application.name=vyapar.ai-backend

# MySQL Live Database Configuration
spring.datasource.url=jdbc:mysql://srv663.hstgr.io:3306/u781846408_trivana_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=u781846408_root
spring.datasource.password=T1r2i3v4
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA settings: update will create/alter tables based on entities
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Data initialization disabled for MySQL (tables already exist)
spring.jpa.defer-datasource-initialization=false
spring.sql.init.mode=never

# H2 Configuration (commented out - using MySQL instead)
# spring.datasource.url=jdbc:h2:mem:vyapardb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=root
# spring.datasource.password=YES
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# JWT configuration (Trivana-style)
app.jwtSecret=ChangeMe123456789012345678901234
app.jwtAccessExpirationMs=900000
app.jwtRefreshExpirationMs=2592000000

# Old JWT config (kept for backward compatibility)
jwt.secret=change-this-to-a-secure-random-string-for-production-use
jwt.expiration-hours=18

# Google OAuth2 Configuration
# Set GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET as environment variables
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:your-client-id}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:your-client-secret}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:8080,http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
